echo $0
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
brew install git
git --version
git config --global user.name "Ryan Pool"
git config --global user.email rlpool06@gmail.com
git config --list
